import { LayoutDashboard } from "lucide-preact";

export default function PostsIcon({
  height,
  width,
}: {
  height: number;
  width: number;
}) {
  return <LayoutDashboard height={height} width={width} />;
}
