import { Settings } from "lucide-preact";

export default function SettingsIcon({
  height,
  width,
}: {
  height: number;
  width: number;
}) {
  return <Settings height={height} width={width} />;
}
